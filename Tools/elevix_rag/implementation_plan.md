# Plan: Dataset Integration

1. Fix `ingest.py` imports (`langchain_text_splitters`).
2. Update `config.py` to point to `d:\projects\Tools\dataset`.
3. Update `ingest.py` to load multiple PDFs.
4. Run ingestion and verify.
